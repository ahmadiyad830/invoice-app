package soft.mahmod.yourreceipt.listeners;

import android.content.DialogInterface;

public interface ListenerDialogWithView {
    void clickOk(DialogInterface dialog);
    void clickCancel(DialogInterface dialog);
}
